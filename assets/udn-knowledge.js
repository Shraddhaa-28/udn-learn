/* Grounded UDN knowledge for the classroom helper.
   Facts are taken from OpenShift 4.22 / OKD Multiple networks docs,
   OpenShift Virtualization networking, and OVN-Kubernetes ClusterNetworkConnect.
   Do not invent APIs that are not in these sources. */
window.UDN_KNOWLEDGE = {
  version: "ocp-4.22",
  sessions: {
    intro: {
      recap: "Click the pictures. Ten sessions. Ask a doubt is bottom right. Start session 01.",
      chips: ["How do I use this lab?", "What will I learn?", "What is Ask a doubt?"],
    },
    why: {
      recap: "Before UDN, OVN-Kubernetes only supported a layer 3 topology on the primary pod network: every pod attaches to that main network and talks by IP, with NetworkPolicy as the filter. UDN adds custom layer 2 and layer 3 segments that are isolated by default, usable as the primary or secondary interface for pods and VMs.",
      chips: ["Why not just NetworkPolicy?", "What did the default network look like?", "What is a UDN?"],
    },
    islands: {
      recap: "Each primary UDN is isolated by default. Pods on the same UDN can talk without NetworkPolicy. Pods on different primary UDNs have no connectivity, so a NetworkPolicy that 'allows' the other namespace has no effect. OVN-Kubernetes handles overlapping UDN subnets.",
      chips: ["Can two UDNs use the same CIDR?", "Why can't A ping B?", "Does NetworkPolicy connect islands?"],
    },
    roles: {
      recap: "Primary is assigned automatically to every pod in the namespace (the default interface). Secondary is assigned only to pods that select the network with the k8s.v1.cni.cncf.io/networks annotation. One primary UDN per namespace; you can attach more than one secondary.",
      chips: ["Primary vs secondary?", "What is the namespace label?", "How do I attach a secondary NIC?"],
    },
    scope: {
      recap: "UserDefinedNetwork is namespace-scoped. ClusterUserDefinedNetwork is cluster-scoped and for administrators; it selects namespaces and can span them. Do not select default or openshift-* namespaces. An empty namespaceSelector matches all namespaces.",
      chips: ["UDN vs CUDN?", "Who should create a CUDN?", "What if the selector is empty?"],
    },
    topo: {
      recap: "Layer 2 creates one logical switch shared by all nodes — used for VM live migration in the same subnet. Layer 3 creates a layer 2 segment per node with a different subnet and routes between them — use cidr plus hostSubnet. Localnet bridges a logical switch to a physical provider network; on CUDN the only supported role is Secondary.",
      chips: ["Layer 2 vs Layer 3?", "What is hostSubnet?", "What is Localnet?", "Can Localnet be primary?"],
    },
    lab: {
      recap: "Create the namespace with k8s.ovn.org/primary-user-defined-network, then the UDN or CUDN CR, then pods. That label can only be set at namespace create time. Assigning an existing namespace that already has pods to a new network is rejected. The CR cannot be modified after create. OVN creates a NetworkAttachmentDefinition when the network is ready.",
      chips: ["What is the create order?", "Can I add UDN to an existing namespace?", "What is a NAD?"],
    },
    gotchas: {
      recap: "DNS lookups for pods resolve to the pod IP on the cluster default network, not the UDN IP. Service DNS still works. UDN pods often cannot reach the in-cluster image registry, so S2I and oc new-app fail. NetworkPolicy cannot enable traffic between different primary UDNs. Kubelet health checks use the default network. The CR is immutable.",
      chips: ["Why is DNS wrong?", "Why can't I pull images?", "Do health checks use the UDN?", "Does NodePort isolate?"],
    },
    virt: {
      recap: "OpenShift Virtualization supports UserDefinedNetwork and ClusterUserDefinedNetwork. Layer 2 overlay is the topology for seamless live migration without NAT; persistent IPs are preserved across reboots and live migration. Localnet is how you attach a VM extra NIC to the physical underlay. Virt does not support Layer 3 as the VM UDN topology in the product docs — Layer2 and Localnet.",
      chips: ["Why Layer 2 for VMs?", "What is Persistent IPAM?", "How do VMs reach a VLAN?"],
    },
    connect: {
      recap: "By default different (C)UDNs are isolated. ClusterNetworkConnect is the admin CR that connects selected primary networks. PodNetwork is full pod-to-pod. ServiceNetwork is ClusterIP only. Connectivity is symmetric and not transitive. CNC cannot connect Secondary or Localnet networks. Overlapping pod subnets cannot be connected.",
      chips: ["What is ClusterNetworkConnect?", "ServiceNetwork vs PodNetwork?", "Can I connect Localnet?", "Is CNC transitive?"],
    },
    field: {
      recap: "Pick a customer story, copy the CR, run it on a throwaway OVN cluster. Order: confirm OVN, birth-label the namespace, apply UDN or CUDN, pods last. Topology: pods Layer3, VMs Layer2 + Persistent, VLAN Localnet Secondary. Never empty CUDN selector. Do not promise registry, S2I, or NetworkPolicy across islands. Two islands that must share an API: CNC ServiceNetwork first. Every concept on this session has a Doc link.",
      chips: ["How do I run this on my cluster?", "SaaS tenants?", "VMs that migrate?", "They already have a VLAN?", "Two apps must share an API?", "Where are the docs?"],
    },
    done: {
      recap: "You finished. Click the four lies on the diploma until they stamp NOPE: registry, empty selector, Localnet primary, NetworkPolicy across islands. Then take the call. The cluster is not clapping.",
      chips: ["What should I say on the call?", "What can I not promise?", "Can I be proud?"],
    },
  },
  chunks: [
    { sessions: ["why"], title: "What UDN adds", keys: "udn user-defined why default layer3 kubernetes design isolation", src: "OCP 4.22 Primary networks — About user-defined networks", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "Before UDN, the OVN-Kubernetes CNI on OpenShift only supported a layer 3 topology on the primary (main) network. Kubernetes design: all pods attach to that main network, communicate by IP, and inter-pod traffic is restricted with NetworkPolicy. That is fine for simple clusters. It is a poor fit when you need custom primary segments for multi-tenant deployments. UDN lets you create custom layer 2 and layer 3 segments. Those segments are isolated by default and can be the primary or secondary network for pods and VMs that use OVN-Kubernetes." },
    { sessions: ["why", "islands"], title: "Isolation without NetworkPolicy", keys: "tenant isolation networkpolicy benefit rhosp security", src: "OCP 4.22 Primary networks — Benefits of a user-defined network", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "UDN gives each namespace its own isolated primary network, similar to tenant isolation in RHOSP. That reduces cross-tenant traffic risk. Isolation can be achieved by putting workloads on different networks, so you do not need complex NetworkPolicy just to separate tenants. NetworkPolicy is still useful for microsegmentation inside one UDN. It is not a replacement for a second network." },
    { sessions: ["why", "roles"], title: "Primary vs secondary network", keys: "primary secondary eth0 default interface multus annotation", src: "OKD Understanding multiple networks", url: "https://docs.okd.io/latest/networking/multiple_networks/understanding-multiple-networks.html", text: "A primary network is the pod's main interface. By default all ordinary traffic goes through it unless you add a route to send some traffic elsewhere. A secondary network is an extra, non-default interface for specific traffic. You can have only one primary network per namespace. You can have multiple secondary networks. UDNs are supported only when OVN-Kubernetes is the cluster CNI." },
    { sessions: ["islands"], title: "Pods on the same UDN vs different UDNs", keys: "same udn talk overlapping subnet cidr isolated no path ping", src: "OCP 4.22 Primary networks — About the UserDefinedNetwork CR", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "You can assign one or more UDNs to a namespace, with a limit of one primary UDN per namespace. Without NetworkPolicy, a pod on a UDN can communicate with other pods on that same UDN. By default those pods are isolated from pods on other UDNs. OVN-Kubernetes handles overlapping UDN subnets. Same CIDR on two primary UDNs does not mean they are the same network — there is still no path between them." },
    { sessions: ["islands", "gotchas"], title: "NetworkPolicy cannot connect two primary UDNs", keys: "networkpolicy different primary isolated not effective allow", src: "OCP 4.22 Primary networks — Limitations of a user-defined network", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "Network policies that enable traffic between namespaces connected to different user-defined primary networks are not effective. Those policies do not take effect because there is no connectivity between the isolated networks. To connect islands you need ClusterNetworkConnect (an OVN-Kubernetes admin CR), not a NetworkPolicy." },
    { sessions: ["roles", "lab", "scope"], title: "Namespace label for primary UDN", keys: "k8s.ovn.org/primary-user-defined-network label birth create namespace cannot update", src: "OCP 4.22 Primary networks — Best practices", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "For a primary network, the namespace must include the label k8s.ovn.org/primary-user-defined-network. That label cannot be updated. It can only be added when the namespace is created. If the label is missing and a pod is created, the pod attaches to the default cluster network. If the label is missing and you create a primary UDN/CUDN for that namespace, the network is not created and a status error is reported. If the namespace has the label but no primary UDN/CUDN yet, pods are not created until that CR exists." },
    { sessions: ["roles"], title: "How secondary attach works", keys: "secondary annotation k8s.v1.cni.cncf.io/networks opt-in eth1", src: "OCP 4.22 ClusterUserDefinedNetwork API", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/network_apis/clusteruserdefinednetwork-k8s-ovn-org-v1", text: "Role Primary: the network is automatically assigned to every pod created in the same namespace. Role Secondary: the network is assigned only to pods that use the k8s.v1.cni.cncf.io/networks annotation to select that network. Localnet on a ClusterUserDefinedNetwork supports Secondary only." },
    { sessions: ["roles", "lab"], title: "Who can create a namespace-scoped UDN", keys: "developer rbac view edit cluster admin create udn", src: "OCP 4.22 Primary networks — Creating a UserDefinedNetwork CR", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "A cluster administrator creates the namespace (and applies the primary-UDN label if it will be a primary network). After that, a user with view and edit RBAC in the namespace can create a UserDefinedNetwork CR there. ClusterUserDefinedNetwork is a different object: it is cluster-scoped and intended for cluster administrators only." },
    { sessions: ["scope", "field"], title: "CUDN is admin-only and cluster-scoped", keys: "clusteruserdefinednetwork cudn administrator security span namespaces", src: "OCP 4.22 Primary networks — About the ClusterUserDefinedNetwork CR", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "ClusterUserDefinedNetwork provides cluster-scoped segmentation and is for administrators only. Used incorrectly it can cause security issues, disruption, or break the cluster network. A CUDN selects namespaces (for example matchLabels) so one network can span many namespaces. Two CUDNs with different selectors are two isolated tenant networks." },
    { sessions: ["scope", "field"], title: "Empty CUDN selector matches every namespace", keys: "empty selector namespaceSelector matchLabels matchExpressions default openshift all namespaces", src: "OCP 4.22 Primary networks — Best practices for ClusterUserDefinedNetwork CRs", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "All namespaces in the cluster are selected when matchLabels is left empty, matchExpressions is left empty, or namespaceSelector is {} with neither matchLabels nor matchExpressions. ClusterUserDefinedNetwork CRs must not select the default namespace or openshift-* namespaces. Always match an explicit tenant label." },
    { sessions: ["topo", "virt"], title: "Layer 2 topology", keys: "layer2 l2 logical switch virtual switch live migration same subnet broadcast storm", src: "OCP 4.22 Primary networks — Layer 2 and layer 3 topologies", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "Layer 2 creates a distributed virtual switch across cluster nodes. VMs and pods connect to that switch and communicate in the same subnet. That is what makes live migration of a VM within the same subnet possible. Specifying Layer2 creates one logical switch shared by all nodes. A layer 2 topology is a single broadcast domain; in a large environment that can cause a broadcast storm. If you omit the Layer2 subnet, you must configure static IPs and port security then only prevents MAC spoofing, not IP spoofing." },
    { sessions: ["topo"], title: "Layer 3 topology and hostSubnet", keys: "layer3 l3 hostsubnet cidr per-node routing broadcast domain pods", src: "OCP 4.22 Primary networks — Layer 2 and layer 3 topologies / Creating UDN", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "Layer 3 creates a unique layer 2 segment for each node, each with a different subnet. Layer 3 routing interconnects those segments so pods and VMs on different nodes can still communicate. Broadcast traffic is scoped to the node, which is how Layer 3 manages large broadcast domains. You must set cidr (the cluster subnet for this UDN) and hostSubnet (the per-node prefix the cidr is split into). Example: cidr 10.100.0.0/16 with hostSubnet 24 means each node gets a /24. For Layer3 the subnets field is mandatory. For IPv6, hostSubnet must be /64." },
    { sessions: ["topo", "virt"], title: "Why VMs use Layer 2 not Layer 3", keys: "vm live migrate nat persistent ip same address layer3 new subnet new ip change address", src: "OCP 4.22 Virtualization — Connecting a VM to a primary UDN", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/virtualization/networking", text: "With Layer 2, OVN-Kubernetes creates an overlay between nodes so VMs on different nodes talk without extra physical plumbing. Layer 2 enables seamless live migration without NAT because persistent IP addresses are preserved across cluster nodes during live migration. OpenShift Virtualization documents Layer2 (and Localnet for underlay) for this. Layer 3 assigns a different subnet per node (cidr + hostSubnet), so a VM that moved node would not keep the same L2 identity. Product note: OpenShift Virtualization only supports the Layer2 and Localnet topologies." },
    { sessions: ["topo", "virt", "field"], title: "Localnet topology", keys: "localnet vlan physical underlay ovs bridge mapping physicalnetworkname secondary only cudn", src: "OCP 4.22 Primary networks — Creating a CUDN for Localnet", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "Localnet connects the secondary network to the physical underlay. That gives east-west cluster traffic and access to services outside the cluster (north-south). It requires an OVS bridge mapping on the nodes first. On ClusterUserDefinedNetwork, topology Localnet creates one logical switch bridged to one provider network. Role Secondary is the only role supported for Localnet. Localnet is not available on the namespace-scoped UserDefinedNetwork CR — use CUDN or a NetworkAttachmentDefinition. As of 4.19, Localnet on CUDN is GA and is the preferred way to connect physical networks to virtual networks." },
    { sessions: ["topo", "virt"], title: "physicalNetworkName must match OVS", keys: "physicalnetworkname ovs bridge mapping network-name wrong cable vlan access id", src: "OCP 4.22 Primary networks — Additional configuration / Localnet best practices", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "spec.network.localnet.physicalNetworkName (CUDN) must match the network-name you configured in the OVS bridge mapping. If it does not match, you are bridging to the wrong physical segment. If several CUDNs share one bridge mapping, they must use the same physicalNetworkName. For VLAN, set localnet.vlan.mode to Access and vlan.access.id to a VID 1–4094. The physical NIC and the datacenter switch must actually carry that VLAN. Avoid overlapping the physical subnet with other interfaces; use excludeSubnets to keep physical IPs out of the CUDN allocation range." },
    { sessions: ["topo", "virt"], title: "Persistent IPAM", keys: "ipam lifecycle persistent reboot migrate virtual workload layer2 not layer3", src: "OCP 4.22 Primary networks — Additional configuration details", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "spec.ipam.lifecycle: Persistent is for virtual workloads so IP addresses survive reboots and migration. CNI assigns them and OVN-Kubernetes programs them. Persistent is only supported when ipam.mode is Enabled. On the feature matrix, Persistent IPs are supported on Layer2 (and Localnet), not on Layer3. For Layer2, if you set ipam lifecycle you must also set subnets." },
    { sessions: ["lab"], title: "Create order", keys: "order namespace label then cr then pods initial assignment existing rejected", src: "OCP 4.22 Primary networks — Overview / Limitations", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "Documented process: 1) administrator creates a namespace with k8s.ovn.org/primary-user-defined-network, 2) the UserDefinedNetwork or ClusterUserDefinedNetwork CR is created, 3) the user creates pods in the namespace. You must create the namespace and network before creating pods. Assigning a namespace that already has pods to a new network, or creating a UDN in an existing namespace that already has pods, is not accepted by OVN-Kubernetes." },
    { sessions: ["lab"], title: "UDN and CUDN CRs are immutable", keys: "cannot modify patch immutable after created", src: "OCP 4.22 Primary networks — Limitations of a user-defined network", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "The ClusterUserDefinedNetwork CR and the UserDefinedNetwork CR cannot be modified after being created. Plan topology, subnets, and role before apply. To change them you delete and recreate (which also means new namespaces/workloads if it was primary)." },
    { sessions: ["lab"], title: "OVN creates the NAD", keys: "nad networkattachmentdefinition networkcreated status", src: "OCP 4.22 Primary networks — User-defined network status", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "You create the UDN or CUDN. OVN-Kubernetes creates a NetworkAttachmentDefinition. Status type NetworkCreated True with reason NetworkAttachmentDefinitionCreated / NetworkAttachmentDefinitionReady means the NAD exists. oc get userdefinednetwork,nad (or clusteruserdefinednetwork). A common failure is SyncError: primary network already exist in namespace." },
    { sessions: ["gotchas"], title: "DNS lookups for pods", keys: "dns core dns nslookup default network wrong ip service kapi", src: "OCP 4.22 Primary networks — Limitations of a user-defined network", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "DNS lookups for pods resolve to the pod's IP address on the cluster default network. Even if the pod is on a UDN, that lookup does not return the UDN IP. DNS lookups for services and external entities work as expected. A pod on a primary UDN can still reach the Kubernetes API and DNS on the cluster default network." },
    { sessions: ["gotchas", "field"], title: "Image registry and S2I from a UDN", keys: "image registry s2i oc new-app openshift-image-registry pull isolated default services", src: "OCP 4.22 Primary networks — Limitations of a user-defined network", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "A UDN pod is isolated from the default network, so most default-network services are inaccessible. It cannot currently access the OpenShift image registry. Source-to-image builds do not work in a UDN namespace. oc new-app and templates that use S2I also fail. Other openshift-*.svc services can be affected. Do not promise in-cluster registry or S2I from a primary UDN without a workaround (external registry the UDN can reach)." },
    { sessions: ["gotchas"], title: "Kubelet health checks", keys: "ready probe health kubelet default network broken primary interface", src: "OCP 4.22 Primary networks — Limitations of a user-defined network", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "Kubelet health checks run on the cluster default network. They do not confirm connectivity of the pod's primary UDN interface. A pod can look healthy (Ready) on the default network while the primary UDN interface is broken. Check the UDN address with oc exec -- ip -br addr, not only Ready." },
    { sessions: ["gotchas"], title: "NodePort isolation is not guaranteed", keys: "nodeport same node different node isolation", src: "OCP 4.22 Primary networks — Limitations of a user-defined network", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "NodePort services on user-defined networks are not guaranteed isolation. Example from the docs: NodePort traffic from a pod to a service on the same node is not accessible, whereas traffic from a pod on a different node succeeds. Do not promise NodePort isolation on a UDN." },
    { sessions: ["topo"], title: "IPv6 hostSubnet and dual-stack", keys: "ipv6 hostsubnet 64 dual stack dual-stack two cidrs", src: "OCP 4.22 Primary networks — Layer 3 topology", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "For Layer3 IPv6, hostSubnet must be /64. Dual-stack UDNs take two CIDRs of different IP families in subnets. Plan this before apply — the CR is immutable." },
    { sessions: ["topo", "virt", "field"], title: "excludeSubnets on Localnet", keys: "excludesubnets physical ips allocation localnet overlap", src: "OCP 4.22 Primary networks — Localnet best practices", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "On Localnet, use excludeSubnets so physical infrastructure IPs are not handed out to pods or VMs. physicalNetworkName must match the OVS bridge mapping. VLAN access id is 1–4094 and must exist on the wire." },
    { sessions: ["gotchas"], title: "IP exhaustion error is unclear", keys: "subnet exhausted failed to create pod sandbox ips", src: "OCP 4.22 Primary networks — Limitations of a user-defined network", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "When a UDN subnet runs out of IPs, new pods fail to start with Warning: Failed to create pod sandbox. That message does not say the subnet is exhausted. Check Events in the namespace for the explicit subnet exhaustion message." },
    { sessions: ["virt"], title: "Primary UDN for VMs — extra limits", keys: "virtctl ssh oc port-forward headless service vm primary udn", src: "OCP 4.22 Virtualization — Connecting a VM to a primary UDN", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/virtualization/networking", text: "The primary UDN replaces the default pod network in that namespace for pods and VMs. Limitations documented for primary UDN VMs: you cannot use virtctl ssh to configure SSH access; you cannot use oc port-forward to the VM; you cannot use headless services to access the VM. You must still add k8s.ovn.org/primary-user-defined-network when you create the namespace." },
    { sessions: ["virt", "topo"], title: "VM extra NIC on the physical network", keys: "vm localnet secondary underlay datacenter vlan clusteruserdefinednetwork", src: "OCP 4.22 Virtualization — Networking", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/virtualization/networking", text: "To connect a VM to physical network infrastructure, configure an OVN-Kubernetes secondary user-defined network with Localnet topology (ClusterUserDefinedNetwork). Configure OVS bridge mappings on the nodes, then attach workloads with the CUDN. Layer 2 overlay UDN is the alternative when you do not have (or do not want) physical underlay, for example public cloud." },
    { sessions: ["connect"], title: "What ClusterNetworkConnect does", keys: "clusternetworkconnect cnc isolated connect islands admin", src: "OVN-Kubernetes — Connecting UserDefinedNetworks", url: "https://ovn-kubernetes.io/features/user-defined-networks/cluster-network-connect/", text: "By default, workloads on different UDNs or CUDNs are completely isolated. ClusterNetworkConnect (CNC) lets cluster administrators connect selected primary networks: full pod-to-pod, service-only, or both. Networks you do not select stay isolated. OVN-Kubernetes creates a transit connect-router and peers it with the selected networks' routers. Feature requires --enable-network-segmentation, --enable-multi-network, and --enable-network-connect." },
    { sessions: ["connect", "field"], title: "PodNetwork vs ServiceNetwork", keys: "podnetwork servicenetwork clusterip nodeport loadbalancer connectivity", src: "OVN-Kubernetes — Connecting UserDefinedNetworks", url: "https://ovn-kubernetes.io/features/user-defined-networks/cluster-network-connect/", text: "PodNetwork: full pod-to-pod communication across the connected networks. ServiceNetwork: ClusterIP services are reachable across those networks, but pods cannot reach each other directly. NodePort and LoadBalancer services are already reachable across UDNs by default. Use ServiceNetwork when full pod connectivity is not needed — tighter boundary: consume APIs through Services without exposing pod IPs. You can set both." },
    { sessions: ["connect"], title: "CNC is symmetric and not transitive", keys: "symmetric non-transitive a-b b-c a-c", src: "OVN-Kubernetes — Connecting UserDefinedNetworks", url: "https://ovn-kubernetes.io/features/user-defined-networks/cluster-network-connect/", text: "Connectivity is symmetric: if UDN-A is connected to UDN-B, B can reach A. Connectivity is not transitive: if A-B and B-C are connected, A is not automatically connected to C. Each pair needs its own ClusterNetworkConnect (or a selector that includes all of them in one CNC)." },
    { sessions: ["connect"], title: "What CNC cannot connect", keys: "secondary localnet overlapping subnets connectsubnets immutable live migration", src: "OVN-Kubernetes — Connecting UserDefinedNetworks — Known limitations", url: "https://ovn-kubernetes.io/features/user-defined-networks/cluster-network-connect/", text: "Only role: Primary networks can be connected. Connecting Secondary UDNs is not supported. Localnet networks cannot be connected with CNC (use bridges). Connecting networks that have overlapping pod subnets is not supported. connectSubnets is immutable once set and must not overlap pod, service, transit, join, masquerade, or node subnets. Live migration and persistent IPs across connected UDNs is not supported. NetworkPolicy peers cannot be selected across connected networks yet." },
    { sessions: ["connect"], title: "How you select networks in CNC", keys: "networkselectors clusteruserdefinednetworks primaryuserdefinednetworks connectsubnets", src: "OVN-Kubernetes — Connecting UserDefinedNetworks", url: "https://ovn-kubernetes.io/features/user-defined-networks/cluster-network-connect/", text: "spec.networkSelectors supports ClusterUserDefinedNetworks (match labels on the CUDN/NAD) and PrimaryUserDefinedNetworks (match namespace labels). spec.connectSubnets is the CIDR OVN uses internally to wire the networks; at most one per IP family; networkPrefix is the per-connected-Layer3-network prefix and limits how many nodes you can grow to. Labels on CUDNs propagate to NADs, which is what the selector matches." },
    { sessions: ["why", "scope"], title: "Do not use NAD instead of UDN for tenant isolation", keys: "networkattachmentdefinition nad tenant security segmentation", src: "OCP 4.22 Primary networks — Best practices for UserDefinedNetwork CRs", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "Tenants should use the UserDefinedNetwork resource, not a hand-written NetworkAttachmentDefinition. Using NAD for tenant isolation can create security risks between tenants. Use NAD only when the isolation you need cannot be done with UDN. OVN still mints a NAD under the UDN for CNI — that generated NAD is expected." },
    { sessions: ["lab", "scope"], title: "Do not put UDN in default or openshift-*", keys: "default namespace openshift platform", src: "OCP 4.22 Primary networks — Best practices for UserDefinedNetwork CRs", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "Do not create a UserDefinedNetwork in the default namespace — that can mean no isolation and cluster security risk. Do not use openshift-* namespaces for a UDN. Same rule for CUDN selectors: never select default or openshift-*." },
    { sessions: ["topo"], title: "Layer2 vs Layer3 one-line API meaning", keys: "topology field api layer2 layer3 localnet", src: "OCP 4.22 ClusterUserDefinedNetwork API", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/network_apis/clusteruserdefinednetwork-k8s-ovn-org-v1", text: "Allowed topology values: Layer3, Layer2, Localnet. Layer3: a layer 2 segment per node, each with a different subnet; layer 3 routing interconnects node subnets. Layer2: one logical switch shared by all nodes. Localnet: based on layer 2, but also connects to an existing configured physical network so workloads get north-south traffic." },
    { sessions: ["field"], title: "Implement UDN on a test cluster", keys: "test cluster hands-on oc apply steps implement lab throwaway ovn first udn how run my cluster", src: "OCP 4.22 Primary networks — Creating a UserDefinedNetwork CR", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "Use a throwaway OpenShift cluster with OVN-Kubernetes, not production. Confirm OVNKubernetes and that the UserDefinedNetwork / ClusterUserDefinedNetwork CRDs exist. Create the namespace with k8s.ovn.org/primary-user-defined-network at birth, apply the UDN or CUDN CR, wait until NetworkReady (OVN creates the NAD), then create pods. The CR cannot be modified after create. Do not add a primary UDN to a namespace that already has pods. Session 10 chips change the CR: first UDN, SaaS CUDN, Layer2 VMs, Localnet VLAN, or CNC ServiceNetwork." },
    { sessions: ["field"], title: "Where the UDN docs are", keys: "docs documentation where link hyperlink official", src: "OCP 4.22 Primary networks", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "Start at OpenShift 4.22 Multiple networks → Primary networks. UserDefinedNetwork and ClusterUserDefinedNetwork APIs are under Network APIs. Primary vs secondary is in OKD Understanding multiple networks. Localnet is Creating a CUDN for a Localnet topology. Persistent IPAM is Additional configuration details. Isolation, DNS, and registry are Limitations of a user-defined network. ClusterNetworkConnect is the OVN-Kubernetes connecting UDNs page. VM live migrate is OpenShift Virtualization networking. Session 10 has a Doc pill for each of those." },
    { sessions: ["field"], title: "SaaS / multi-tenant recommendation", keys: "saas tenant multi-tenant layer3 cudn label", src: "OCP 4.22 Primary networks — Benefits / CUDN diagram", url: "https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/multiple_networks/primary-networks", text: "For isolated tenants that are pods: ClusterUserDefinedNetwork, Layer3, role Primary, namespaceSelector matchLabels for that tenant, namespaces created with k8s.ovn.org/primary-user-defined-network. Overlapping CIDRs are fine because isolation is the network. Tell the customer: registry and S2I may not work; NetworkPolicy will not reach the other tenant; empty selector is unsafe." },
    { sessions: ["field"], title: "Two apps must share an API", keys: "two apps api share servicenetwork cnc", src: "OVN-Kubernetes — Connecting UserDefinedNetworks — Best practices", url: "https://ovn-kubernetes.io/features/user-defined-networks/cluster-network-connect/", text: "Keep two primary networks. Create ClusterNetworkConnect with ServiceNetwork so they can consume ClusterIP Services without exposing every pod IP. Add PodNetwork only if they truly need pod-to-pod. Do not try to fix this with NetworkPolicy across primary UDNs — that policy does not take effect." },
    { sessions: ["intro"], title: "How to use UDN Lab", keys: "how use lab classroom play diagram quiz guide intro", src: "UDN Lab classroom", url: "", text: "Left: a diagram you click (pods, Ping, chips). Play types fake oc output for that picture. The example CR uses # as YAML comments — the cluster ignores them. Each session has one check question. Ask a doubt (bottom right) answers from OpenShift 4.22 and OVN-Kubernetes docs for this session. Ten sessions after the intro, then a finish page." },
    { sessions: ["intro"], title: "What you will learn", keys: "what learn path sessions overview", src: "UDN Lab classroom", url: "", text: "You will learn why UDN exists, island isolation, primary vs secondary, UDN vs CUDN, Layer 2 / Layer 3 / Localnet, create order, DNS/registry/policy traps, VMs and live migrate, ClusterNetworkConnect, and what to say on a customer call. The pictures matter more than memorizing YAML." },
    { sessions: ["intro"], title: "What Ask a doubt is", keys: "ask a doubt chatbot docs helper", src: "UDN Lab classroom", url: "", text: "Ask a doubt is a helper grounded in OpenShift 4.22 Multiple networks / Primary networks, Virtualization networking, and OVN-Kubernetes ClusterNetworkConnect. It is not the public internet. Ask in your own words. If it cites a Doc link, that is the source." },
    { sessions: ["done", "field"], title: "Call checklist after the lab", keys: "call checklist remember promise proud graduation", src: "UDN Lab classroom", url: "", text: "On the next UDN ticket: namespace labeled at create with k8s.ovn.org/primary-user-defined-network? UDN (one namespace) or CUDN (many, admin, never empty selector)? Topology: pods Layer3, VMs Layer2 + Persistent, VLAN Localnet Secondary. Do not promise in-cluster registry, S2I, or NetworkPolicy across islands. Two islands that must share an API: ClusterNetworkConnect with ServiceNetwork first. You can be proud. You can still say you will check the docs." },
  ],
};
